# Feature Expansion Matrix

Living status document for the 2026-07 complete feature expansion. Updated at
every integration checkpoint. Contracts: `docs/expansion-contracts.md`.
Brief: `docs/feature-expansion-handoff-2026-07-30.md`.

Legend: status = planned | in-progress | integrated | verified.
"Verified" requires the exact receipts named in the Verification column.

| # | Feature | Source surfaces | Migration | Tests | Runtime verification | Status |
|---|---------|-----------------|-----------|-------|----------------------|--------|
| 0a | Event schema versioning + tolerant decode | `ClipboardModels.swift`, `ClipboardArchiveWriter.swift`, all readers | v1 lines decode as version 1; `archive-format.json` marker | old→new fixture decode, unknown-content-type skip, benchmark-schema drift guard | synthetic mixed-version archive read | integrated (`a77819a`) |
| 0b | Suppression unification + perf fixes (ledger cache, incremental retention prune, copy-back re-capture fix) | `ClipboardSuppression.swift` (new), `ClipboardArchiveReader/Searcher/Pruner/DerivedIndex`, `main.swift`, `ClipboardPanelController.swift` | none (behavior-preserving) | suppression parity tests, prune-increment tests, ledger-cache tests, timing bound | 50k synthetic scale benchmark (pending Slice 2 gate) | integrated (Slice 1 merge) |
| 0c | Synthetic fixture builders (every content type + migration path) | `Tests/.../SyntheticFixtures.swift` | n/a | fixture round-trips | n/a | integrated (`a77819a`) |
| 1 | Quick picker + configurable global shortcut | new `QuickPickerPanelController`, hotkey manager in MenuBar target; settings keys | settings keys default disabled | shortcut persistence, picker filter model | isolated AppKit gesture: open, arrow-navigate, copy-back, dismiss; <100 ms @50k | integrated-pending-review (`slice2-quick-picker`): 60/60 tests (6 suites), 28 checks ok, local-only ok, release app build+sign ok; 4-variant /tmp gesture matrix pass — filtered query (1/5), commit copies to isolated pasteboard with archiveEnabled=true and event count 5→5 after a poll tick (no re-capture), escape leaves pasteboard untouched; picker open 41 ms at 50,065 synthetic events (gate <100 ms); automation never registers the Carbon hotkey |
| 2 | Full-archive search + filters (date/app/type/window) | `ClipboardDerivedIndex` structured search, `ClipboardPanelController` archive mode | index `user_version` bump + rebuild | structured-search tests, filter tests | synthetic archive-wide search in isolated app | integrated-pending-review (`slice3-archive-search`): 76/76 tests (7 suites, 16 new in `ClipboardIndexSearchTests`), 28 checks ok, local-only ok; index schema v2 (`user_version=2`, `content_hash`+`preview` columns, `captured_at`/`content_hash` indexes) with rebuild-on-mismatch (v0→v2 fixture + failed-rebuild-preserves-prior + no-op second ensure receipts); sqlite3 `-json` stdin runner round-trips 0x1F/0x1E + injection-shaped content exactly; legacy CLI `search()` SQL moved off argv onto stdin (security log item); `event(withID:)` single-day-file fetch with id-shape validation + suppression gate; panel scope control + date/app filters + 250 ms debounce/generation state machine; 5-run /tmp harness matrix — all-history browse 4/5 visible with manufactured ledger-drift clip AND hand-built doNotIndex clip absent, query `checklist` 1 hit (doNotIndex clip also contains the token, still absent), Safari+This-Year filter 1 hit, empty state, working-window regression 5 clips with `state=working-window` — result JSONs + PNG snapshots under `/tmp/clipboard-slice3-h1..h5/` |
| 3 | Pins/favorites with retention protection | annotations store (new `ClipboardAnnotations.swift`), pruner exemption, panel UI | new sidecar file, absent = none | pin round-trip, prune-exemption, bulk-exemption | isolated app pin → prune → survives | integrated-pending-review (`slice4-pins-tags`): 107/107 tests (12 suites; new `AnnotationsStoreTests` 12 + `RetentionPinExemptionTests` 5 + `RedactorAnnotationTests` 6), 28 checks ok, local-only ok; sidecar store 0700/0600 atomic + symlink-rejecting + corrupt-aside + future-version READ-ONLY (pins still protect, mutations throw, file byte-identical — /tmp receipt h6 `annotationsFileByteIdentical=true`, status "Saved by a newer version…"); pinned OUTSIDE the retention limit in both prune paths + `includePinned` override + last-occurrence annotation sweep (redactor uses content_hash index post-filtered through the ledger, reader-scan fallback); CLI `prune --include-pinned` prints `exempt_pinned` (dry-run parity receipt); /tmp harness h1/h1b: seed 14 → pin 2 oldest → enforce limit 10 → `exemptPinnedEvents=2, prunedEvents=2, keptCountedEvents=10, pinnedEventsSurvived=true` + PNG pin badges; fresh-root h5 renders with zero annotations IO (`annotationsDirectoryExists=false`); unpin resets the retention estimate via the panel's `onArchiveMutation(pinRemoved:)` funnel; results under `/tmp/clipboard-slice4-h*/` |
| 4 | Duplicate grouping (count, first/last, expand) | `content_hash` index column, panel grouping | index version bump | grouping unit tests | isolated app with seeded duplicates | integrated-pending-review (`slice4-pins-tags`): grouping is presentation AFTER the existing filter predicate in BOTH scopes (This Window in-memory; All History via new meta-only `metaRows` capped 5,000 → read-time suppression → Swift grouping, NO SQL GROUP BY; cap surfaced in footer); no index version bump needed — `content_hash` landed with schema v2 in Slice 3, this slice only ADDS queries (`occurrenceIDs`, `metaRows`) and the `content_hash` column to search/browse SELECTs; `DuplicateGroupingTests` (7) cover counts/first/last/representative, empty-hash never groups, filter-then-group composition, drift-excluded counts, blocked excluded, limit + cap; /tmp harness h2/h3/h4/h7: ×3 duplicates collapse to `group:3` with "3 copies · first … · latest …" + ×3 capsule + chevron, expand splices indented occurrence rows, collapsed-group copy commits the NEWEST occurrence with event count 8→8 after a capture-poll tick (no re-capture) — group-copy PNG + result JSONs; delete disabled on collapsed group rows (Slice 5 owns bulk); "Group duplicates" checkbox persisted as tolerant settings key `historyGroupDuplicates` (default off) |
| 5 | Bulk management (selection/date/app/type/sensitivity) + truthful reclaim preview | bulk engine over redactor, panel multi-select UI, dry-run preview | none | dry-run == execute parity, count/byte truth tests | synthetic bulk delete with before/after receipts | integrated-pending-review (`slice5-bulk-privacy`): `ClipboardBulkEngine` — preview/execute share ONE `run(_:dryRun:)` path into the promoted pruner core (`pruneCore`: `exemptContentHashes` + `restrictToDayRange` filename-date scan bound + `reclaimedBytes`; batched `delete(eventIDs:)` BEFORE tombstoning, replacing the core's trailing full rebuild); truthful reclaim = stat'ed body files + signed original−tombstone line delta, identical in both modes — `BulkEngineTests` (10) incl. on-disk byte-delta truth (`bytesBefore-bytesAfter == reclaimedBytes` over `raw/`), every criterion, pinned exemption + includePinned, ledger reason `bulk-<sorted keys>`, drift no-double-count, tombstone field parity with redactor, day-range bound (out-of-range day file byte-identical + unscanned); History multi-select delete rerouted through the engine (truthful count+bytes in the confirm; all-pinned selection gets the separate include-pinned confirm); bulk sheet: criteria popups, Preview, edit-invalidates-Delete, include-pinned confirm quoting exempted count, background exec; CLI `bulk --dry-run/--json`; /tmp receipt `clipboard-slice5-h1-bulk`: previewMatchesExecute=true field-for-field, deleteEnabledAfterEdit=false, 5→1 events |
| 6 | Rich formats (image/file/RTF/color/link) | capture branch in `main.swift`, writer/reader body types, detail rendering | event schema v2 fields, tolerant decode | per-type round-trip, size-cap block, containment | isolated capture of synthetic rich pasteboard | integrated-pending-review (`slice6-rich-formats`): 182/182 tests (19 suites; new `RichContentTests` 20), 28 checks ok, local-only ok; first-class types image/file-reference/rich-text/color (titled links stay `url` + `richContent.kind=="link"`); ONE tolerant `richContent` field, `currentSchemaVersion=2` with PER-LINE stamping — text lines stay byte-identical v1 shape (pinned literal test) and `rawContentPath` stays plain-text-only forever; capture priority fileURL>png/tiff>rtf>color>url+name>string behind `captureRichContent` (default on) with 10 MiB `richImageMaxBytes` cap checked on `data.count` BEFORE any decode/hash → blocked reason `image_exceeds_size_cap:<n>b:limit:<cap>b`; file lists store metadata ONLY (>100 spills full list to `<id>.json`), file NAMES in FTS body / full paths metadata-only (not-found-by-path-token pinned), `ClipboardDerivedIndex.indexBody` shared by upsert AND rebuild (parity pinned); per-kind `ClipboardCaptureDedup` shared by poll + event-based copy-back (original representations: stored bytes + derived second image rep, RTF+string, NSURLs+paths, NSColor+hex, URL+url-name+string); detail rendering per kind with `RichThumbnailProvider` (CGImageSource ≤1024 px, ~32 MB NSCache, off-main generation-guarded — never a full decode); redactor/pruner core/retention path delete `richContent.bodyPath` + null it in tombstones; health adds rich extensions + `richContentEvents` + `orphanedRichBodyFiles`; body files deleted on NDJSON append failure (fixes pre-existing text-body leak); privacy: rich flows through the SAME filter gates (concealed-type block, secret detector on plain fallback, store-no-index rich → `.restricted` never upserted — fixtures per contract 10); /tmp receipts `clipboard-slice6-*`: 5-kind `richcapture` matrix (kindStored/bodyFileExists/copyBackByteEqual=true/no re-capture 1→1), cap-block `{blockedReason: image_exceeds_size_cap:80000b:limit:65536b, eventCount:0, richBodyFilesOnDisk:0}`, history list + 5 per-kind detail PNGs (red thumbnail, monospaced file list with "(missing)", swatch+hex+space, attributed RTF, title+URL), picker render with per-kind icons |
| 7 | Clip actions (plain copy, strip formatting, URL cleanup, whitespace, join, edit-before-copy) | new `ClipTransformations.swift` (core), panel/picker actions | none | pure-function transform tests (fixture table) | isolated app action gestures | planned |
| 8 | Tags, collections, snippets | annotations store, panel UI, picker sections | sidecar versioned | tag/collection/snippet round-trips | isolated app tagging flow | integrated-pending-review (`slice4-pins-tags`): tags via detail-pane NSTokenField (single selection, completions from `allTags()`, commit on end-editing, trim/drop-empty/case-insensitive-dedupe normalization); collections popup above search (All Clips | Pinned | Snippets | named | New Collection…) with client-side hash filtering in both scopes + collection order = `contentHashes` order + detail "Add to Collection" checkmark pulldown + full CRUD/`moveItem` API (drag-reorder UI deferred one slice per the design cut-line — API ships); snippets: snippet ⇒ pinned invariant (unpin clears snippet after a warning), panel "Save as Snippet…" context item, quick-picker top section only when query empty (title + text.badge.star, disabled when zero live occurrences) with COMMIT-TIME resolution newest-live-occurrence → shared no-re-capture copy path; store/round-trip tests in `AnnotationsStoreTests` (incl. Slice 5 placeholder fields round-tripped untouched); /tmp harness h8: picker renders Snippets section, Return commits the snippet — `pasteboardMatchesSelection=true`, event count 5→5 with capture ON, open 38 ms; PNG + result JSON under `/tmp/clipboard-slice4-h8-picker-snippet/` |
| 9 | Privacy upgrades (per-app rules, manual sensitivity, expiring clips, timed private mode, blocked-event explanations) | `ClipboardPrivacyFilter`, settings rules, annotations expiry, menu UI | settings keys default to current behavior | allowed/blocked/false-positive/non-retention fixtures per rule | isolated rule exercise | integrated-pending-review (`slice5-bulk-privacy`): `.restricted` = stored/visible/never-searchable via `ClipboardSuppression.isIndexExcluded` (second named predicate in the one gate file; `isSuppressed` unchanged) wired into upsert delete-instead-of-insert, rebuild skip, CLI searcher skip, and the ingestor's annotations-cache pre-check for re-copies (+`manual-restricted` flag); per-app rules (lowercased keys, raw-mode lossless round-trip, unknown=block fail-closed) with doc'd precedence type-denylist → built-ins (normal cannot override) → explicit rules (`app_rule_block:<bundleid>`) → legacy lists → secret detector, store-no-index writes `.restricted`+`app-rule-no-index` via the writer's new privacyLabel path AND keeps the legacy exclusion entry (older builds stricter); expiry sweeper (`nextDue` = annotations cache only; launch + 30-min timer + surface-open checks, never the capture poll, no read-time hiding) executes occurrence ids through the bulk engine `includePinned:true` reason `expired-sensitive`; timed private mode returns BEFORE the pasteboard read + exit resync, FIXING the pre-existing pause retro-capture bug (`ClipboardCaptureGate` pure core); `PrivacyRulesTests` (13) + `RestrictedSemanticsTests` (7) + `ExpirySweeperTests` (6) + `PrivateModeTests` (7); /tmp receipts: h3 privatemode {storedDuring:0, blockedDuring:0, storedAfterResume:0, storedAfterNewCopy:1}, h4 mark-restricted {indexRowsForRestrictedHashes:0, row0ShowsRestrictedBadge:true, 5 clips still visible}, h5 expire-past+sweep {sweptHashes:1, deletedEvents:1, 5→4}, h6 rules-card PNG (legacy row renders Block, explicit row Store-don't-index) |
| 10 | Storage/health dashboard (sizes, counts, repair, cleanup) | `ClipboardArchiveHealth` extensions, new settings card | none | health computation tests | isolated dashboard render | integrated-pending-review (`slice5-bulk-privacy`): separate Storage & Health WINDOW (menu Maintenance item replaces the health NSAlert; Local Storage card gains the button) — Overview off-main (bodyFileBytes, eventFileCount, oldest/latest, restrictedEvents, pinned/tagged/expiring, indexUserVersion, annotationsBytes + existing fields), Recent Blocked Items via new `reader.recentBlockedEvents` + `ClipboardBlockedEventExplainer` (per-reason-family humanization incl. `app_rule_block`), Maintenance (rebuild w/ inline count+time receipt, Verify Integrity = quick_check + health summary), Cleanup (prune-by-age preview→delete through the bulk engine, edit invalidates, Bulk Cleanup… sheet); CLI `health` prints the new fields; `HealthDashboardExtensionTests` (4); /tmp receipt h2: settled render PNG + healthFacts {storedEvents:5, blockedEvents:3, indexUserVersion:2} + 3 humanized blocked explanations |
| 11 | Encrypted backup/export + restore/import | new `ClipboardBackup.swift` (CryptoKit AES-GCM + KDF), settings entry, CLI cmds | backup format v1, versioned header | round-trip, wrong-passphrase, tamper, partial-restore tests | synthetic export→wipe→import in isolated root | integrated-pending-review (`slice8-encrypted-backup`): CLIPBAK1 container — PBKDF2-HMAC-SHA256 (CommonCrypto, calibrated ~250 ms, hard floor 600k; receipt: 2,083,333 iters calibrated on this machine; import bounds 100k..10M) + HKDF-SHA256 manifest/payload subkeys; plaintext KDF header is the sealed manifest's AAD (wrong passphrase ≡ tamper, one error); per-entry 4 MiB chunk boxes with positional AAD magic+backupID+entryIndex+chunkIndex (drop/reorder/splice/trailing all hard-fail); allowlist enumeration (raw/, deletion-ledger/, annotations/, manifests/, archive-format.json; `meta/settings.json` opt-in, default OFF; derived index EXCLUDED; non-allowlisted files reported by name); three-phase import: authenticate+stage into hidden 0700 `.backup-staging-*` (per-chunk tag+AAD+framing+per-entry sha256, hostile paths rejected pre-write) → ONE planner shared by dry-run and execute → journaled commit LEDGER-FIRST (fail-closed) → bodies → day files (atomic replace, just-in-time pre-image refresh) → annotations → marker → settings → full index rebuild (failure = "run repair-index", data intact), pre-image rollback + next-entry stale-staging recovery; merge table exact: local ledger authoritative ("skipped (deleted here)" + matching staged bodies vetoed), ledger union + locally-live newly-suppressed callout, divergent body hash = whole-import pre-write abort, blocked idempotent, annotations pinned=OR(earlier pinnedAt)/tags=union/title+sensitivity=local-wins/expiry=earlier, collections union-by-id + "(imported)" rename; CLI `backup create/inspect/restore` (`--include-settings/--merge/--dry-run/--apply-settings/--json`; passphrase via readpassphrase(3) or `--passphrase-stdin`, NEVER argv/env/logs); `ClipboardBackupUIController` (save/open panels, secure-field sheets, preview window with full plan counts, off-main crypto w/ progress) + 2 Local Storage buttons + 2 Maintenance menu items + post-import mutation hook; 191/191 tests (20 suites; 35 new in `ClipboardBackupTests`+`ClipboardBackupMergeTests`: empty/mixed/multi-chunk byte-identical round-trips incl. 0600/0700 modes, wrong-passphrase zero-writes, bit-flip header/manifest/chunk + equal-size chunk swap (positional AAD) + truncation ×3 + trailing + iteration bounds + future version + hostile paths ×7, full merge matrix, fault-injected rollback at ALL 8 commit steps with byte-identical tree + settings restore, simulated-crash stale-staging recovery, autoreleasepool memory watermark <40 MiB peak on 48 MiB body), 28 checks ok, local-only ok; `scripts/backup-roundtrip-check.sh`: seed→create→inspect JSON counts→restore `diff -r` byte-identical + 600/700 modes→wrong-pass zero-writes→merge dry-run==execute field-for-field; UI preview-sheet PNG skipped (would require a new automation branch in shared main.swift — CLI receipts suffice per brief); settings-screen render receipt `/tmp/clipboard-slice8-settings/settings-card.png` proves the app boots with the new wiring |
| 12 | Daily-use polish (shortcut config UI, launch-at-login via SMAppService, states, onboarding, version/update info) | Settings/onboarding controllers, `SMAppService` integration | settings keys | login-item state tests where feasible | isolated render QA + keyboard-only walkthrough | planned |

## Integration checkpoints

| Date | Commit | Contents | Gate receipts |
|------|--------|----------|---------------|
| 2026-07-30 | `43ab766` (baseline) | pre-expansion baseline | 28/28 tests, 25 checks ok, package+validate+local-only+rollback ok |
| 2026-07-30 | Slice 1 (`a77819a` + perf merge) | schema versioning, tolerant content types, fixtures, `--index-path`, suppression unification, ledger cache, incremental retention, copy-back re-capture fix | 41/41 tests (3 suites), 28 checks ok, package+validate+local-only+rollback ok |
| 2026-07-30 | Slice 2 merge + reseed fix | quick picker, global shortcut config, shortcut recorder, direct-paste opt-in, retention periodic reseed (security finding) | 60/60 tests (6 suites), 28 checks ok, package+validate+local-only+rollback ok; picker gesture matrix + 41 ms @50k receipts in row 1 |
| 2026-07-30 | `slice3-archive-search` (pending merge) | index schema v2 + structured search/browse/distinctSourceApps, stdin JSON runner, legacy search argv→stdin fix, reader by-id fetch, panel All History scope + filters, harness scope/filter/drift flows | 76/76 tests (7 suites), 28 checks ok, local-only ok; 5-run harness matrix receipts in row 2 |
| 2026-07-30 | `slice4-pins-tags` (pending merge) | annotations sidecar store + occurrence resolver, pruner pin exemption (+`includePinned`), redactor last-occurrence annotation sweep, index `metaRows`/`occurrenceIDs`/content_hash in results, duplicate grouping both scopes, pin/tags/collections panel UI (⌘P, badges, token field, collections popup), quick-picker snippets section, CLI `--include-pinned`, settings `historyGroupDuplicates` | 107/107 tests (12 suites), 28 checks ok, local-only ok; 9-run /tmp harness matrix receipts in rows 3/4/8 (`/tmp/clipboard-slice4-h*/`) |
| 2026-07-30 | `slice5-bulk-privacy` (pending merge) | `.restricted` semantics + `isIndexExcluded`, per-app privacy rules + filter precedence, annotations sensitivity/expiry activation, promoted pruner core (day-range bound, reclaimedBytes, pre-tombstone batched index delete), `ClipboardBulkEngine`, `ClipboardExpirySweeper`, timed private mode + pause retro-capture fix (`ClipboardCaptureGate`), Storage & Health dashboard + `ClipboardBlockedEventExplainer` + `recentBlockedEvents`, bulk sheet + History delete reroute, Mark Sensitive UI + eye.slash badges, App Privacy Rules settings card + downgrade fail-closed save, settings-store ISO-8601 date decode fix, CLI `bulk`/`sweep-expired` + extended `health` | 156/156 tests (18 suites; 6 new files), 28 checks ok, local-only ok; 6-screen /tmp harness matrix receipts in rows 5/9/10 (`/tmp/clipboard-slice5-h*/`) |

| 2026-07-30 | `slice8-encrypted-backup` (pending merge) | CLIPBAK1 encrypted backup format (PBKDF2 calibrated/600k floor + HKDF subkeys, sealed manifest w/ header AAD, positional chunk AAD), allowlist exporter (0600 partial → fsync → atomic rename), three-phase importer (staging, shared dry-run/execute planner, journaled ledger-first commit, pre-image rollback, stale-staging recovery), full merge semantics, CLI `backup create/inspect/restore`, `ClipboardBackupUIController` + settings buttons + Maintenance items, `backup-roundtrip-check.sh` | 191/191 tests (20 suites; 35 new), 28 checks ok, local-only ok, round-trip harness receipts in row 11 |
| 2026-07-30 | `slice6-rich-formats` (pending merge) | rich content types + schema v2 per-line stamping, `richContent` field + `ClipboardRichContent`/`ClipboardRichPayload`/`ClipboardCaptureDedup`, writer rich bodies + append-failure body cleanup, ingestor image cap + shared `indexBody`, `reader.richBody`/`fileList`, redactor/pruner/health rich-body handling + orphan counter, poll rich classification + event-based copy-back, panel 6-segment filter + per-kind detail rendering + `RichThumbnailProvider`, picker/panel shared `systemSymbolName`, settings `captureRichContent`/`richImageMaxBytes` + UI toggle, CLI health fields, `futureVersionLine` retarget image→hologram | 182/182 tests (19 suites), 28 checks ok, local-only ok; 12-run /tmp harness matrix receipts in row 6 (`/tmp/clipboard-slice6-*`) |

## Discovered issues (running log)

| Found | Issue | Disposition |
|-------|-------|-------------|
| 2026-07-30 scout | Retention prune = full rewrite + full FTS rebuild on every capture in recent-10/50 | fix in Slice 1 (contract 9) |
| 2026-07-30 scout | Panel copy-back re-captures the copied clip as a new event | fix in Slice 1 (contract 2) |
| 2026-07-30 scout | Suppression checks inconsistent across readers (ledger vs doNotIndex) | unify in Slice 1 (contract 6) |
| 2026-07-30 scout | History search is preview-only, never searches body text or FTS | addressed by Slice 3 |
| 2026-07-30 scout | `uiVisibleUntil` written but never read | keep writing (compat), document as legacy |
| 2026-07-30 scout | CLI has no `--index-path` flag; prune/redact use default index with custom `--archive-root` | fix in Slice 1 |
| 2026-07-30 scout | `check-local-only.sh` does not scan `Sources/ClipboardArchiveChecks` | fix in Slice 1 |
| 2026-07-30 scout | `scale-benchmark.sh` duplicates event schema by hand | drift guard in Slice 1 |
| 2026-07-30 scout | NDJSON appends unprotected between app and CLI monitor running concurrently | document; single-writer guidance; ledger/index already locked |
| 2026-07-30 review | Incremental retention: crash between ledger write and index delete left stale index rows | fixed `15eb65f` (fail-closed ordering: index delete first) |
| 2026-07-30 review | Body-file deletion counts overstated when file already missing (both prune paths) | fixed `15eb65f` |
| 2026-07-30 security | liveEventCountEstimate undercounts with a concurrent second writer (GUI+CLI) → retention silently unenforced | fixed post-Slice-2: forced reseed every 10 captures |
| 2026-07-30 security | Split-string URL literals in checks binary set an evasion precedent vs check-local-only.sh | fixed: plain literals + explicit `https://example.com/` allowlist in the gate |
| 2026-07-30 security | Legacy `ClipboardDerivedIndex.search()` passes user query SQL via argv (pre-existing; contract 3 wants stdin) | fixed in Slice 3: SQL streams over stdin (`-batch`), raw-text CLI output preserved |
| 2026-07-30 security | `PrivacyLabel.restricted` exists but is outside the suppression gate; future manual-sensitivity work must define its semantics | assigned to Slice 5 privacy rules; contract note |
| 2026-07-30 review | CRITICAL: History-panel delete never dirties quick-picker cache → deleted content copyable from stale in-memory events | fixed post-Slice-3: panel `onArchiveMutation` closure → markQuickPickerCacheDirty |
| 2026-07-30 review | CRITICAL: hotkey conflict at launch has no durable surface (settings window not yet created; menu status overwritten) | fixed: persistent ⚠ menu item + pending-failure push into Settings on creation |
| 2026-07-30 review | Settings save hides the window in the same run-loop turn as a conflict label write | fixed: save-time registration failure shows an alert |
| 2026-07-30 review | One malformed shortcuts entry reset ALL settings to defaults (capture off, exclusions lost) | fixed: FailableDecodable per-entry tolerance + tests |
| 2026-07-30 design | PRE-EXISTING PRIVACY BUG: pause exit never resyncs lastChangeCount → the last item copied during a pause is retro-captured on resume | FIXED in Slice 5: gate-exit resync on manual pause toggle, timed-pause expiry, and private-mode exit (`ClipboardCaptureGate` + `resyncPasteboardStateWithoutIngesting`); receipts: `PrivateModeTests.testPauseExitResyncPreventsRetroCapture` + /tmp h3 privatemode `{storedDuring:0, blockedDuring:0, storedAfterResume:0}` |
| 2026-07-30 review | CRITICAL: lockf is per-(process,inode) — same-process threads bypass the index lock; Slice 3's background search queue racing main-thread upserts reproducibly lost index rows during rebuild | fixed: per-path in-process NSLock layered under the file lock in withExclusiveLock |
| 2026-07-30 review | ensureCurrentSchema spawned an extra sqlite3 subprocess per capture on the main thread | fixed: process-wide verified-schema path cache |
| 2026-07-30 review | Save-time shortcut alert re-fired on every unrelated settings save | fixed: alert only on newly discovered failure |
| 2026-07-30 review | structuredSearch/browse read-time filter checks ledger only, not a persisted privacy-label column; safe today because doNotIndex rows never physically enter the index (upsert deletes, rebuild skips) | accepted + documented; REVISITED in Slice 5: no label column added — restricted rows keep the same never-physically-enter invariant (upsert delete-instead-of-insert incl. manual overrides, rebuild skip), so read-time ledger filtering remains sufficient |
| 2026-07-30 slice5 | ClipboardSettingsStore.load used a plain JSONDecoder while save writes ISO 8601 dates → any persisted date (timed pause) reset the whole settings file to defaults on next launch | fixed in Slice 5: ISO 8601 decode first with plain fallback; round-trip pinned by `PrivacyRulesTests.testSettingsStoreRoundTripsRulesAndPersistedDates` |
| 2026-07-30 review | ⌘P in the History window fired during text edits, discarding uncommitted tag/search input | fixed post-Slice-5: field-editor focus guard falls through to super |
| 2026-07-30 review | All-History search + collection filter post-filtered a 200-hit pool → older collection members could silently miss | fixed post-Slice-5: query path deep-reaches (5000, matching metaRows) when collection scope or grouping is active |
| 2026-07-30 review | Bulk sheet Close during an in-flight delete deallocated the controller → archive mutated but caches never invalidated | fixed: strong completion capture + Close disabled while work runs |
| 2026-07-30 review | Settings file with MIXED date encodings failed both whole-file decode attempts → silent factory reset un-blocking privacy rules; a numeric addedAt dropped its block rule (fail-open) | fixed: per-field flexible date decode for pauseUntil/privateModeUntil/rule addedAt + tests |
| 2026-07-30 review | ClipboardCaptureGate exists + is unit-tested but pollPasteboard reimplements gating inline (tests give false confidence) | fixed post-Slice-6: pollPasteboard consumes ClipboardCaptureGate.decision; the switch performs only side effects |
| 2026-07-30 review | Restricted eye.slash badge only on This Window; picker + menu show restricted clips unlabeled | fixed post-Slice-6: picker rows swap to eye.slash + "Restricted" metadata; menu items get eye.slash images |
| 2026-07-30 slice6 design | Forward-compat gap: an OLD build redacting/pruning a v2 rich event rewrites the line WITHOUT `richContent` (its decoder drops the field) and never deletes the rich body — the body file is orphaned on disk | accepted per design (rawContentPath separation makes it harmless data-at-rest, never resurfaced); health now counts `orphanedRichBodyFiles` so the gap is visible; recovery is manual cleanup after upgrading |
| 2026-07-30 slice6 design | PRE-EXISTING QUIRK: the ingestor re-sorts `event.sensitivityFlags` IN MEMORY after the writer already appended the line — a divergence pattern between the returned event and the persisted line (currently benign because the writer sorts too) | logged per design decision; not fixed silently — any future post-write mutation must rewrite the line or move before the append |
| 2026-07-30 slice6 | Secret detector's `single-high-entropy-value` heuristic false-positived on ordinary single-file copies (a lone file path IS one long high-entropy token), blocking legitimate file-reference captures | fixed in Slice 6: file-list captures drop ONLY the bare-token entropy flag; all pattern flags (tokens/keys inside names or paths) still block — pinned by `RichContentTests.testSingleFilePathDoesNotTripEntropyHeuristicButTokenNamedFileStillBlocks` |
| 2026-07-30 review | CRITICAL: merge-restore resurrected rich bodies (image/RTF/file-list spill) of locally-deleted events as orphaned disk files — deleted-here suppression tracked rawContentPath only | fixed: richContent.bodyPath joins deletedHereBodyPaths + `RichBackupSuppressionTests` regression (live-reproduced by review, re-verified fixed) |
| 2026-07-30 review | Capture-gate refactor deferred private-mode-expiry cleanup/status while a pause was also active | fixed: skipPaused branch clears expired privateModeUntil immediately (gating unaffected) |
| 2026-07-30 review | Restricted-badge check re-read the annotations store once per menu item per rebuild | fixed: one restrictedContentHashes snapshot per rebuildMenu |
| 2026-07-30 slice6 | Harness seeding appended fixtures newest-first, but `recentItems` derives order from append position within a day file → seeded history rendered oldest-first (live capture unaffected; `seedSyntheticUIFixtures` has the same latent quirk) | rich seeder appends oldest-first; existing seeder left untouched (its receipts are order-insensitive) — flagged for a later cleanup |
